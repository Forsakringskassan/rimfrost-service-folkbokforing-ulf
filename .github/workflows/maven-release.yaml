name: Bundle Maven Release

on: [workflow_dispatch]

jobs:
  call-workflow:
    uses: Forsakringskassan/github-ulf/.github/workflows/bundle-maven-release.yaml@build-manifest-instead-of-buildx
    secrets:
      PACKAGES_RW_ACTOR: ${{ secrets.PACKAGES_RW_ACTOR }}
      PACKAGES_RW_TOKEN: ${{ secrets.PACKAGES_RW_TOKEN }}
